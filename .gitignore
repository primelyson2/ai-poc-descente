# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# App secrets & wallets
config.yaml
asta-quality-agent.yaml
asta-loop.yaml
wallets/
.secrets/

# region 별 model 후보 (환경별 파일) — 추적 템플릿은 models.txt.example, 실제 파일은 무시
models.txt

# 내부 보안 문서 (공개 금지)
Guide_Security_info.md

# Local runtime and agent artifacts
.hermes/
.pytest_cache/
logs/
reports/

# OS
.DS_Store

# Terraform (OCI Resource Manager 는 .tf/schema.yaml 만 필요 — 로컬 산출물 제외)
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log
